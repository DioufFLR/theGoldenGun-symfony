<!-- component -->
<!-- Header -->
<header>
    <!-- navbar and menu -->
    <nav class="bg-black">

        <div class="flex justify-between items-center py-6 px-10 container mx-auto">

            <div>
                <img src="{{ asset('assets/img/logoreel.png') }}" alt="logo" width="200">
            </div>

            <div>

                <div class="hover:cursor-pointer sm:hidden">
                    <span class="h-1 rounded-full block w-8 mb-1 bg-gradient-to-tr from-indigo-600 to-green-600"></span>
                    <span class="h-1 rounded-full block w-8 mb-1 bg-gradient-to-tr from-indigo-600 to-green-600"></span>
                    <span class="h-1 rounded-full block w-8 mb-1 bg-gradient-to-tr from-indigo-600 to-green-600"></span>
                </div>
                <div class="flex items-center">

                    <ul class="sm:flex space-x-4 hidden items-center">
                        <li><a href="{{ path('app_main') }}" class="text-white hover:text-rose-400 text-md ">Accueil</a>
                        </li>
                        <li><a href="{{ path('app_product') }}"
                               class="text-white hover:text-rose-400 text-md ">Produits</a></li>
                        <li><a href="#" class="text-white hover:text-rose-400 text-md ">Contact</a></li>

                        {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                            <li><a href="{{ path('app_admin') }}"
                                   class="text-white hover:text-rose-400 text-md ">CRUD</a></li>
                        {% endif %}

                        {# Cart icons and count articles #}
                        {% if app.session and app.user and app.user.cart and app.user.cart.cartItems|length > 0 %}
                            <li>
                                <a href="{{ path('cart_show') }}" class="text-white text-md">
                                    <svg
                                            class="w-5 h-4 text-gray-800 dark:text-white hover:text-rose-400"
                                            aria-hidden="true"
                                            xmlns="http://www.w3.org/2000/svg"
                                            fill="none"
                                            viewBox="0 0 18 20"
                                    >
                                        <path
                                                stroke="currentColor"
                                                stroke-linecap="round"
                                                stroke-linejoin="round"
                                                stroke-width="2"
                                                d="M6 15a2 2 0 1 0 0 4 2 2 0 0 0 0-4Zm0 0h8m-8 0-1-4m9 4a2 2 0 1 0 0 4 2 2 0 0 0 0-4Zm-9-4h10l2-7H3m2 7L3 4m0 0-.792-3H1"
                                        />
                                    </svg>
                                    <span class="text-white">
                                        {{ app.user.cart.cartItems|length }}
                                    </span>
                                </a>
                            </li>
                        {% endif %}

                    </ul>

                    <div class="md:flex items-center hidden space-x-4 ml-8 lg:ml-12">

                        {% if app.user %}
                            <h1 class="text-text-gray-600  py-2 hover:cursor-pointer px-4 rounded text-white bg-gradient-to-r from-rose-600 to-rose-400 hover:shadow-lg transition ease-in-out delay-150 hover:-translate-y-1 hover:scale-110 duration-300">
                                <a href="{{ path('app_logout') }}">Déconnexion</a>
                            </h1>
                        {% else %}
                            <h1 class="text-text-gray-600  py-2 hover:cursor-pointer px-4 rounded text-white bg-gradient-to-r from-rose-600 to-rose-400 hover:shadow-lg transition ease-in-out delay-150 hover:-translate-y-1 hover:scale-110 duration-300">
                                <a href="{{ path('app_login') }}">Connexion</a>
                            </h1>
                            <h1 class="text-text-gray-600  py-2 hover:cursor-pointer px-4 rounded text-white bg-gradient-to-r from-rose-600 to-rose-400 hover:shadow-lg transition ease-in-out delay-150 hover:-translate-y-1 hover:scale-110 duration-300">
                                <a href="{{ path('app_register') }}">S'inscrire</a>
                            </h1>
                        {% endif %}

                    </div>
                </div>
            </div>
        </div>
    </nav>
</header>
